name: sample

on:
  push:
    branches:
      - master

jobs:
  test:
    name: Do Something
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v2.3.3

      - name: Run Code
        run: python Slearnings/ExpediaAgain.py
